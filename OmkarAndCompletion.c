#include <stdio.h>
int main()
{
    int i, n, t;
    scanf("%d", &t);
    while (t--)
    {
        scanf("%d", &n);
        for (i = 1; i <= n; i++)
            printf(" 5 ");
        printf("\n");
    }
    return 0;
}
